# Copier ce fichier en « .env » (dans mcp/) et le remplir.
# Le fichier .env est ignoré par git : il ne quitte jamais ce PC.
SUPABASE_URL=https://esvhefwihcjfvvvazykd.supabase.co
# La clé « publishable » — la même que dans app/.env (VITE_SUPABASE_KEY)
SUPABASE_KEY=
# Le compte Supabase DÉDIÉ à Claude (membre du foyer, jamais la service key)
CLAUDE_EMAIL=
CLAUDE_PASSWORD=
